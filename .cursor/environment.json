{
  "disableAllMcpServers": true,
  "install": "set -eu; uv_dir=\"${TMPDIR:-/tmp}/cursor-uv-0.12.12\"; mkdir -p \"$uv_dir\"; curl --fail --location --proto '=https' --tlsv1.2 https://astral.sh/uv/0.12.12/install.sh | UV_UNMANAGED_INSTALL=\"$uv_dir\" sh; export PATH=\"$uv_dir:$PATH\"; uv sync --frozen; uv run --no-sync python -m unittest discover -s tests"
}
